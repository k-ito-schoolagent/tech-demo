# 参加のしかた

FORM — Interactive Science Lab へようこそ。エンジン、歯車、回路、光、天体。**ものの仕組みが好きな人なら、だれでも参加できます。** プログラムが書けなくても、説明文の改善やアイデアの提案で参加できます。

| やること | むずかしさ | 必要なもの |
|---|---|---|
| [1. issue で話してみる](#1-issue-で話してみる) | やさしい | GitHub のアカウントだけ |
| [2. 説明を直す・前提を足す](#2-説明を直す前提を足す) | ふつう | GitHub のアカウントとブラウザだけ |
| [3. 新しいデモをつくる](#3-新しいデモをつくる) | じっくり | パソコン、Node.js 22 以上、テキストエディタ |

---

## GitHub がはじめての方へ：ここで練習してください

issue（イシュー）やプルリクエスト（PR）を一度も使ったことがなくても、まったく問題ありません。**このリポジトリは、issue や PR の練習に使ってもらってかまいません。** まちがえても消したりやり直したりできますし、メンテナーが一緒に直します。

### 練習1：issue を書いてみる（5分）

1. GitHub にログインして、このリポジトリの **Issues** タブ → **New issue** を開く
2. **「練習：はじめての issue」** を選ぶ
3. 自己紹介や、デモにしてほしい仕組みをひとこと書いて **Create** を押す

### 練習2：はじめてのプルリクエスト（15分）

[CONTRIBUTORS.md](CONTRIBUTORS.md) に自分の名前を1行足してみましょう。ブラウザだけでできます。

1. [CONTRIBUTORS.md](CONTRIBUTORS.md) を開き、右上の鉛筆マーク（Edit this file）を押す
   - 「Fork this repository」と出たら、そのボタンを押します。あなたのアカウントに複製（フォーク）ができて、そこで編集します
2. いちばん下に `- @あなたのID　好きな機械：〇〇` のように1行書く
3. 右上の **Commit changes...** → 説明を書いて **Propose changes**
4. 次の画面で **Create pull request** を押す

プルリクエストを送ると、GitHub Actions が自動でテストとビルドを走らせます（緑のチェックが付けば OK）。メンテナーが確認して取り込むと（マージ）、あなたの名前が一覧に載ります。

> わからないことがあれば、issue に「ここで止まりました」と書いてください。質問も立派な参加です。

---

## 1. issue で話してみる

テンプレートがあるので、空欄を埋めるだけで書けます。

- **新しいデモのリクエスト**：「歯車列の減速比を見たい」「ダイオードの整流を見たい」
- **説明・モデルのまちがいの報告**：「この説明は不正確では」「この式の前提が書かれていない」
- **不具合の報告**：「この端末で表示されない」「ボタンが押せない」

自分でつくる人も、先に issue を立てて「このデモを作ります」と書いておくと、同じデモを二人が別々に作ってしまうのを防げます。

---

## 2. 説明を直す・前提を足す

ページの説明文は各デモの HTML（例：`bjt.html`）に、計算モデルの前提は [README.md](README.md) の「モデルの前提」にあります。GitHub でファイルを開いて鉛筆マークから編集すれば、ブラウザだけでプルリクエストを送れます。

- 分かりにくい言い回しを、分かりやすくする
- 「このモデルでは再現していないこと」を足す（学ぶ人の誤解を防ぐ大事な情報です）
- 参考資料を足す
- 計算モデルのテストを足す（`src/*.test.js`）

---

## 3. 新しいデモをつくる

1つのデモは「ページ（HTML）」「3D と操作（JS）」「計算モデル」「テスト」の4つのファイルでできています。見本の **単振り子**（`pendulum.html`、`src/pendulum.js`、`src/pendulum-model.js`、`src/pendulum-model.test.js`）をコピーしてつくります。

### 使うもの

| 用途 | もの |
|---|---|
| 動かす | [Node.js](https://nodejs.org/) 22 以上（npm も一緒に入ります） |
| 書く | テキストエディタ（VS Code など） |
| 3D | [three.js](https://threejs.org/)（npm で自動的に入ります。ドキュメントは [threejs.org/docs](https://threejs.org/docs/)） |
| 開発サーバーとビルド | [Vite](https://vite.dev/)（npm で自動的に入ります） |
| アイコン | [Lucide](https://lucide.dev/) |
| テスト | Node.js の標準テストランナー（`node --test`） |

### 手順

1. **issue を立てる**：「〇〇のデモを作ります」と書いておきます
2. **フォークしてクローンする**
   ```bash
   git clone https://github.com/あなたのID/tech-demo.git
   cd tech-demo
   git switch -c add-gear-train
   npm install
   ```
3. **雛形をつくる**（id は英小文字・数字・ハイフン）
   ```bash
   npm run new-demo -- gear-train "歯車列"
   ```
   見本をコピーした4つのファイルができて、`demos.json` に登録されます。ナビと一覧ページには自動で出ます
4. **`demos.json` の自分の行を書く**：`title`・`summary`（一覧に出る紹介文）・`category`（機械・電気・半導体・力学・光・熱 など）・`authors`
5. **開発サーバーで見ながら書く**
   ```bash
   npm run dev
   ```
   表示された URL の `/gear-train.html` を開きます。保存すると自動で画面が更新されます
6. **計算モデルとテストを書く**（`src/gear-train-model.js` と `src/gear-train-model.test.js`）
   - 画面に出す数値は、描画のコードではなくモデルのファイルで計算します
   - 「必ず成り立つはずのこと」（保存則、境界での連続性、既知の値との一致など）をテストにします
   ```bash
   npm test
   ```
7. **3D と操作を書く**（`src/gear-train.js` と `gear-train.html`）
   - 3D の舞台は `createStage()` が用意します（カメラ、光、視点ボタン、部品のクリック、ラベル）
   - 見出し、操作パネル、説明、「モデルの前提」の欄は HTML に書きます
8. **サムネイルをつくる**：`/gear-train.html?embed&thumb` を開くと、3Dだけの表示になり、右下に「サムネイルを保存」ボタンが出ます。見せたい角度にドラッグしてからボタンを押し、保存された `gear-train.webp` を `public/thumbs/` に置きます（トップの一覧に出る画像です）
   - トップページの大きな3D表示（ショーケース）は、各デモの `?embed` 表示をそのまま埋め込んでいます。見本と同じ `createStage()` を使っていれば、何もしなくてもショーケースに出ます
9. **README の「モデルの前提」と「検証」に自分のデモを足す**
10. **ビルドできるか確かめる**
   ```bash
   npm run build && npm run preview
   ```
11. **プルリクエストを送る**：テンプレートのチェック項目を埋めてください。GitHub Actions がテストとビルドを自動で確認します

### デモづくりの約束

- **モデルの前提を必ず書く。** 何を計算していて、何を省略・誇張しているか（例：粒子の数や速さは誇張、摩擦は無視）。これがこのラボのいちばん大事な約束です
- **計算はテストで確かめる。** 見た目がそれらしく動くだけでなく、数値が正しいことを確かめます
- **ドラッグ以外でも操作できるようにする。** 視点ボタン、キーボードで動くスライダーなど（見本と同じ作りならそのまま満たせます）
- **動きを減らす設定を尊重する。** OS の「視差効果を減らす」がオンのときは停止状態から始めます
- **実在の製品は模式化する。** 特定製品の寸法・ロゴ・図面をそのまま使いません
- スマートフォンの幅でも崩れないか確かめます

---

## プルリクエストが取り込まれるまで

1. プルリクエストを送る → GitHub Actions がテストとビルドを自動で確認
2. メンテナーが内容を確認して、コメントで質問や提案をすることがあります（責めているのではなく、一緒によくするためのやりとりです）
3. 問題がなければマージします。数分で公開ページに自動で反映されます

---

## ライセンス

プルリクエストを送った時点で、プログラムは [MIT License](LICENSE)、文章は [CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/deed.ja) で公開することに同意したものとします。くわしくは [LICENSE-CONTENT.md](LICENSE-CONTENT.md) を読んでください。

参加するすべての人は [行動規範](CODE_OF_CONDUCT.md) を守ってください。
